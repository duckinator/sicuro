language: ruby
rvm:
  - 1.9.3
  - 2.0.0
  - rbx-19mode
  - jruby
#  - ruby-head

script: bundle exec rspec spec

notifications:
  email:
    on_success: change # default: change
    on_failure: change # default: always

matrix:
  allow_failures:
    - rvm: rbx-19mode
    - rvm: jruby
